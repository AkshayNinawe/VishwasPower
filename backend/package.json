{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "npx nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node -e \"try { require('puppeteer').executablePath() } catch(e) { process.env.NODE_TLS_REJECT_UNAUTHORIZED='0'; require('child_process').execSync('npx puppeteer browsers install chrome', {stdio: 'inherit'}) }\"",
    "puppeteer:install": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 npx puppeteer browsers install chrome",
    "puppeteer:check": "node -e \"console.log('Puppeteer executable:', require('puppeteer').executablePath())\""
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.10.0",
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.13.1",
    "multer": "^2.0.2",
    "nodemon": "^3.1.9",
    "openai": "^5.7.0",
    "pdfkit": "^0.17.2",
    "puppeteer": "^24.32.0"
  },
  "puppeteer": {
    "skipDownload": false,
    "executablePath": null
  }
}
